// Declaración.
let texto = "Hello";
let text  = "helLo";

// Leer un texto de un punto a otro.
let part1 = texto.slice(2, 3); console.log(part1);
let part2 = texto.substring(2, 3); console.log(part2);

// Desde la posición 2 devuelve 3 caracteres.
let part3 = texto.substr(2, 3); console.log(part3);